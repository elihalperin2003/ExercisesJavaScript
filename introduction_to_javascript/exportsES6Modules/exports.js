export const sum = (a, b) => a + b;

export const greet = (name) => `hello ${name}`;

export const greet2 = (name) => `hello ${name}`;

export const pi = 3.14152;

export const name = "Loopy Abu Mishel";

// export default function mult(num) {
//   return num * 2;
// }

export default { name: "Mishel" };

import { add } from "./exports2.js";

export const c = add;

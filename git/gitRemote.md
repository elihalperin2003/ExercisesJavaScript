1. התחברות למאגר ששמור ברשת
2. origin
3. ניהול גרסאות עדיין יעיל בלי שמירה ברשת ובעבודה לבד

1. git remote add origin <url.repo>
2. git push
3. המחשב לא יבין מאיפה לקבל את המאגר

1. git clone
2. git pull
3. ההבדל הוא בין הורדת פרויקט חדש לבין עדכון הגירסה האחרונה של הפרויקט

1. git remote, SSH-keys
2. התחברות ללא נתינת פרטים
3. השיטה השנייה

תרגיל 5

1. git clone <url.repo>
2. git pull
3. git push
4. git remote add origin <url.repo>
5. gut push